on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: GitHub Checkout
        uses: actions/checkout@v3
      # checkout the private repo containing the action to run
      - name: Checkout GitHub Action Repo
        uses: actions/checkout@v3
        with:
          repository: GlitchSecure/jekyll4-deploy-gh-pages
          ref: refs/heads/main
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          path: .github/actions/jekyll4-deploy-gh-pages
      # Caches and reuses ruby gems to decrease build time
      - name: Bundler Cache
        uses: actions/cache@v3
        with:
          path: vendor/bundle
          key: ${{ runner.os }}-gems-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-gems-
      # Builds the site in a docker container and pushes to gh-pages branch and seperate repo
      - name: Build & Deploy to GitHub Pages
        uses: ./.github/actions/jekyll4-deploy-gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ secrets.GITHUB_REPOSITORY }}
          GITHUB_REPOSITORY_PUBLIC: "GlitchSecure/public-website"
          GITHUB_PUSH_TOKEN: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          GITHUB_ACTOR: ${{ secrets.GITHUB_ACTOR }}
